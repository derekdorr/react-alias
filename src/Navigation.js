/**
 * Alias for the <nav> tag. The Navigation element defines a set of links for
 * user naviation.
 *
 * @module Navigation
 * @func
 * @since 1.0.0
 * @param {Object} props
 */

import alias from './alias';

const Navigation = alias('nav', 'Navigation');

export default Navigation;
