/**
 * Alias for the <figcaption> tag. The FigureCaption element defines a caption for
 * a Figure element.
 * 
 * @module FigureCaption
 * @func
 * @since 1.0.0
 * @param {Object} props
 */

import alias from './alias';

const FigureCaption = alias('figcaption', 'FigureCaption');

export default FigureCaption;
