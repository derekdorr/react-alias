/**
 * Alias for the <br> tag. The LineBreak element breaks inline content to the
 * next line.
 *
 * @module LineBreak
 * @func
 * @since 1.0.0
 * @param {Object} props
 */

import alias from './alias';

const LineBreak = alias('br', 'LineBreak');

export default LineBreak;
