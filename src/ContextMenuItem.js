/**
 * Alias for the <menuitem> tag.  The ContextMenuItem defines an element of a
 * ContextMenu.
 * 
 * @module ContextMenuItem
 * @func
 * @since 1.0.0
 * @param {Object} props
 */

import alias from './alias';

const ContextMenu = alias('menuitem', 'ContextMenuItem');

export default ContextMenu;
