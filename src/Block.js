/**
 * Alias for the <div> tag.  The Block component defines generic, non-semantic
 * block-level content.
 * 
 * @module Block
 * @func
 * @since 1.0.0
 * @param {Object} props
 */

import alias from './alias';

const Block = alias('div', 'Block');

export default Block;
