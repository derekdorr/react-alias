/**
 * Alias for the <li> tag. The ListItem element denotes a child of an ordered or
 * unordered list.
 * 
 * @module ListItem
 * @func
 * @since 1.0.0
 * @param {Object} props
 */

import alias from './alias';

const ListItem = alias('li', 'ListItem');

export default ListItem;
