/**
 * Alias for the ol tag. The OrderedList element provides a container for an
 * ordered list.
 *
 * @module OrderedList
 * @func
 * @since 1.0.0
 * @param {Object} props
 */

import alias from './alias';

const OrderedList = alias('ol', 'OrderedList');

export default OrderedList;
